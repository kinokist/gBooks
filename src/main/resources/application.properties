spring.application.name=gBooks
implementation 'org.mariadb.jdbc:mariadb-java-client:3.3.2'

spring.datasource.url=jdbc:mariadb://localhost:3307/gbooks
spring.datasource.username=gbooks
spring.datasource.password=New1234!
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# UTF-8 인코딩 설정
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# XML 매퍼 경로 수정 (mappers → mapper)
mybatis.mapper-locations=classpath:mapper/**/*.xml

# application.properties
spring.output.ansi.enabled=always

# DTO 클래스 패키지로 변경
#mybatis.type-aliases-package=com.example.flutterproject.product.dto.out